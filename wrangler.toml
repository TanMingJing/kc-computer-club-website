name = "kc-computer-club-website"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
# Note: Cloudflare Pages is primarily for static sites.
# For Next.js with server-side rendering, Vercel is recommended.
# See DEPLOYMENT_OPTIONS.md for alternatives.

# If deploying as static-only (SSG), use this:
pages_build_output_dir = ".next/static"
